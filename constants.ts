/**
 * Application Constants
 *
 * NOTE: Mock data has been removed in favor of real API data.
 * Guest users now see real achievements/leaderboard with unlocked: false.
 *
 * Only GUEST_USER is exported from here for backward compatibility.
 * Use src/utils/guestMode.ts for guest-related functionality.
 */

export { GUEST_USER, GUEST_PROMPTS } from './src/utils/guestMode';
